// Функция должна проверять, является ли введенное значение undefined,
// но она работает некорректно. Можете ли вы исправить код?

function isUndefined(value) {
    return value === undefined;
}

console.log(isUndefined(undefined));